import { ValidateRangNosDirective } from './validate-rang-nos.directive';

describe('ValidateRangNosDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateRangNosDirective();
    expect(directive).toBeTruthy();
  });
});
